{
  "name": "atombomb",
  "version": "0.0.2",
  "description": "A designed UI for aria2c.",
  "main": "src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "packwin32": "electron-packager . AtomBomb --win --out ../AB --arch=ia32 --app-version=0.0.2 --electron-version=6.0.1 --icon=ab.ico --ignore=node_modules --asar",
    "packwin64": "electron-packager . AtomBomb --win --out ../AB --arch=x64 --app-version=0.0.2 --electron-version=6.0.1 --icon=ab.ico --ignore=node_modules --asar"
  },
  "author": "Kevin Li",
  "license": "Apache-2.0",
  "dependencies": {
    "mousetrap": "^1.6.3",
    "string-random": "^0.1.3",
    "electron": "^6.0.7"
  }
}
